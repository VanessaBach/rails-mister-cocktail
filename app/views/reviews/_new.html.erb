<h3 class="text-light">Add a review</h3>
<div class="text-light">
	<%= simple_form_for [cocktail, review] do |f| %>
	  <%= f.input :rating, collection: 0..5 %>
	  <%= f.input :content %>
	  <%= f.submit class: "btn btn-primary"%>
	<% end %>
</div>